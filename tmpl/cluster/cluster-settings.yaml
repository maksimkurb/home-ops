---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-settings
  namespace: flux-system
data:
  METALLB_LB_RANGE: ${BOOTSTRAP_METALLB_LB_RANGE}
  METALLB_TRAEFIK_ADDR: ${BOOTSTRAP_METALLB_TRAEFIK_ADDR}
  SVC_BLOCKY_ADDR: ${BOOTSTRAP_BLOCKY_ADDR}
  SVC_HOME_ASSISTANT_ADDR: ${BOOTSTRAP_HOME_ASSISTANT_ADDR}
  SVC_PLEX_ADDR: ${BOOTSTRAP_PLEX_ADDR}
  SVC_QBITTORRENT_ADDR: ${BOOTSTRAP_QBITTORRENT_ADDR}