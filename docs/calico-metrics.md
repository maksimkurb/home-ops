# Calico Metrics

Historical note: this cluster no longer tracks a Calico deployment in Git. Current networking docs and manifests should be based on the active Cilium/K3s setup instead.

Keep this file only as a breadcrumb for older cluster history. Do not use it as an implementation runbook unless Calico manifests are restored to the repository.
