---
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: metallb-pool-main
  namespace: metallb-system
spec:
  addresses:
    - "${METALLB_LB_RANGE}"

---

apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: metallb-pool-postgres
  namespace: metallb-system
spec:
  addresses:
    - "${METALLB_LB_POSTGRES_RANGE}"

---

apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: metallb-pool-traefik
  namespace: metallb-system
spec:
  addresses:
    - "${METALLB_LB_TRAEFIK_RANGE}"

---

apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: metallb-pool-blocky
  namespace: metallb-system
spec:
  addresses:
    - "${METALLB_LB_BLOCKY_RANGE}"

---

apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: metallb-pool-home-assistant
  namespace: metallb-system
spec:
  addresses:
    - "${METALLB_LB_HOME_ASSISTANT_RANGE}"

---

apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: metallb-pool-mosquitto
  namespace: metallb-system
spec:
  addresses:
    - "${METALLB_LB_MOSQUITTO_RANGE}"

---

apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: metallb-pool-plex
  namespace: metallb-system
spec:
  addresses:
    - "${METALLB_LB_PLEX_RANGE}"

---

apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: metallb-pool-qbittorrent
  namespace: metallb-system
spec:
  addresses:
    - "${METALLB_LB_QBITTORRENT_RANGE}"

---

apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: metallb-l2-advertisement
  namespace: metallb-system