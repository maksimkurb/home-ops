---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
namespace: security

secretGenerator:
  - name: glauth
    files:
      - server.toml=server.sops.toml
generatorOptions:
  disableNameSuffixHash: true
