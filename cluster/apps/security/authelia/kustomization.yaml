---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
namespace: security
resources:
  - secret.sops.yaml
  - helm-release.yaml
configMapGenerator:
  - name: authelia
    files:
      - config/configuration.yml
generatorOptions:
  disableNameSuffixHash: true
