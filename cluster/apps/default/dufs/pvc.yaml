---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: dufs-nfs-media
spec:
  capacity:
    storage: 1Mi
  accessModes:
    - ReadWriteMany
  storageClassName: dufs-nfs-media
  nfs:
    server: "${NFS_PROXMOX_ADDR}"
    path: /tank/media
  mountOptions: ["nfsvers=4.2", "nconnect=16", "soft", "retrans=5", "noatime"]
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dufs-nfs-media
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: dufs-nfs-media
  resources:
    requests:
      storage: 1Mi

---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: dufs-nfs-filehosting
spec:
  capacity:
    storage: 1Mi
  accessModes:
    - ReadWriteMany
  storageClassName: dufs-nfs-filehosting
  nfs:
    server: "${NFS_PROXMOX_ADDR}"
    path: /tank/filehosting
  mountOptions: ["nfsvers=4.2", "nconnect=16", "soft", "retrans=5", "noatime"]
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dufs-nfs-filehosting
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: dufs-nfs-filehosting
  resources:
    requests:
      storage: 1Mi
