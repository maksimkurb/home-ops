---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: minecraft
  namespace: gaming
spec:
  interval: 5m
  chart:
    spec:
      # renovate: registryUrl=https://itzg.github.io/minecraft-server-charts/
      chart: minecraft
      version: 3.6.4
      sourceRef:
        kind: HelmRepository
        name: minecraft-server-charts
        namespace: flux-system
      interval: 5m
  values:
    image: itzg/minecraft-server
    imageTag: java17
    persistence:
      dataDir:
        enabled: true
        existingClaim: minecraft-data
    minecraftServer:
      eula: "TRUE"
      type: "SPIGOT"
      difficulty: normal
      whitelist: ${SECRET_MINECRAFT_PLAYERS_WHITELIST}
      serviceType: LoadBalancer
      loadBalancerIP: "${SVC_MINECRAFT_ADDR}"
      onlineMode: false