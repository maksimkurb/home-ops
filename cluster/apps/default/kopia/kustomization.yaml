apiVersion: kustomize.config.k8s.io/v1
kind: Kustomization
namespace: default
resources:
  - kopia-secret.sops.yaml
  - helm-release.yaml
configMapGenerator:
  - name: kopia
    files:
      - repository.config
generatorOptions:
  disableNameSuffixHash: true
