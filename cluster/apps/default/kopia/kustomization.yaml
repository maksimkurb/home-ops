---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
resources:
  - pv.yaml
  # - maintenance
  # - sync
  - web
namespace: default
configMapGenerator:
  - name: kopia
    files:
      - repository.config
generatorOptions:
  disableNameSuffixHash: true
