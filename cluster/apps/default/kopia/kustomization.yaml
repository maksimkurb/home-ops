apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - kopia-secret.sops.yaml
  - helm-release.yaml
configMapGenerator:
  - name: kopia
    files:
      - repository.config
generatorOptions:
  disableNameSuffixHash: true
