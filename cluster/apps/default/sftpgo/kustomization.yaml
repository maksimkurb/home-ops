apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
resources:
  - helm-release.yaml
  - pvc.yaml
  - secret.sops.yaml
namespace: default
configMapGenerator:
  - name: sftpgo
    files:
      - config.json
generatorOptions:
  disableNameSuffixHash: true
