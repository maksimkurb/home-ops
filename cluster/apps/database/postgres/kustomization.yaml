---
apiVersion: kustomize.config.k8s.io/v1
kind: Kustomization
namespace: database
resources:
  - https://raw.githubusercontent.com/zalando/postgres-operator/v1.9.0/manifests/postgres-operator.yaml
  - https://raw.githubusercontent.com/zalando/postgres-operator/v1.9.0/manifests/api-service.yaml
  - https://raw.githubusercontent.com/zalando/postgres-operator/v1.9.0/manifests/operator-service-account-rbac.yaml
  - postgres-cluster.yaml
  - zalando-env.yaml
  - secret.sops.yaml
  - configmap.yaml
patches:
  - target:
      kind: Deployment
      name: postgres-operator
      version: apps/v1
    patch: |
      - op: add
        path: "/spec/template/metadata/annotations"
        value:
          configmap.reloader.stakater.com/reload: "postgres-operator"
