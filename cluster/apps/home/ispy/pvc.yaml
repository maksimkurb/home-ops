---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: ispy-media-nfs
spec:
  capacity:
    storage: 1Mi
  accessModes: ["ReadWriteMany"]
  storageClassName: ispy-media-nfs
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: "${NFS_PROXMOX_ADDR}"
    path: "${NFS_PROXMOX_CCTV_ISPY}"
  mountOptions: ["nfsvers=4.2", "nconnect=16", "soft", "noatime"]
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ispy-media-nfs
  namespace: default
spec:
  accessModes: ["ReadWriteMany"]
  storageClassName: ispy-media-nfs
  resources:
    requests:
      storage: 1Mi
