---
apiVersion: v1
kind: Service
metadata:
  name: pve-node-exporter-external
  namespace: monitoring
spec:
  type: ExternalName
  externalName: ${PVE_HOST_IP}
  ports:
    - name: metrics
      protocol: TCP
      port: 9100
      targetPort: 9100
